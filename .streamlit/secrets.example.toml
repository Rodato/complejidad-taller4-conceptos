# Copia este archivo a .streamlit/secrets.toml y rellena los valores.
# secrets.toml está gitignoreado — NUNCA lo subas al repo.

[gcp_service_account]
type = "service_account"
project_id = "complejidad-496215"
private_key_id = "..."
private_key = "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
client_email = "detective-redes@complejidad-496215.iam.gserviceaccount.com"
client_id = "..."
auth_uri = "https://accounts.google.com/o/oauth2/auth"
token_uri = "https://oauth2.googleapis.com/token"
auth_provider_x509_cert_url = "https://www.googleapis.com/oauth2/v1/certs"
client_x509_cert_url = "https://www.googleapis.com/robot/v1/metadata/x509/..."

# Recomendación: usar el MISMO sheet que la app del Grupo B (3. Inmobilliario/),
# pero en una pestaña distinta. El service account ya tiene permisos sobre ese documento.
# La pestaña "taller4" la crea la app automáticamente la primera vez.
[sheets]
url = "https://docs.google.com/spreadsheets/d/<MISMO_ID_QUE_GRUPO_B>/edit"
worksheet = "taller4"
